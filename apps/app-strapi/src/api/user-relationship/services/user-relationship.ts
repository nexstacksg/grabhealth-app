import { factories } from '@strapi/strapi';

export default factories.createCoreService('api::user-relationship.user-relationship', ({ strapi }) => ({
  // Custom service methods can be added here
}));