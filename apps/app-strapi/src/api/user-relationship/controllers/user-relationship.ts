import { factories } from '@strapi/strapi';

export default factories.createCoreController('api::user-relationship.user-relationship', ({ strapi }) => ({
  // Custom controller methods can be added here
}));